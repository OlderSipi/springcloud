server:
  port: 80

eureka:
  client:
    register-with-eureka: false
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka/

ribbon:
  ReadTimeout: 5000  #建立后读取资源的时间
  ConnectTimeout: 5000  #建立连接的时间

logging:
  level:
    com.fxz.springcloud.service.PaymentFeignService: debug